============
Installation
============

At the command line::

    $ easy_install ipinfo_django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ipinfo_django
    $ pip install ipinfo_django
